<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="dev.yamacraft.garbagebox.app" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".ui.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.AndEngineActicity"
            android:label="@string/app_name"/>
        <activity android:name=".ui.NavigationDrawerActivity"
            android:label="@string/app_name"/>
        <activity android:name=".ui.ScribeActivity"
            android:label="@string/app_name"/>
        <activity android:name=".ui.IntentCheckActivity"
            android:label="@string/app_name"/>
    </application>

</manifest>
